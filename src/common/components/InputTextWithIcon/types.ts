import { InputProps } from "../InputText/types";

export type InputTextWithIconProps = InputProps & {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  icon?: any;
  inputClassName?: string;
};
