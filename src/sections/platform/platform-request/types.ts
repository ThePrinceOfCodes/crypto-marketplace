export interface tokenProps {
  swappable: boolean;
  isApproved: boolean;
  platform_id: string;
  updatedAt: string;
  createdAt: string;
  address: string;
  onChain: boolean;
  price: number;
  name: string;
}
